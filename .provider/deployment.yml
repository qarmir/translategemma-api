schema_version: 1
compose_file: .provider/compose.yml
public_service:
  name: api
  port: 8000
  healthcheck:
    path: /health
    timeout_seconds: 120
deployment:
  strategy: recreate
  startup_timeout_seconds: 300
